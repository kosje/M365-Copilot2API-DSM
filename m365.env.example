# M365-Copilot2API 本机启动环境变量示例
# 复制本文件为 m365.env 即可生效（run.bat 会自动读取）

# 监听地址：仅本机用 127.0.0.1（默认）；如需同局域网其他设备访问改 0.0.0.0:4141
M365_LISTEN=127.0.0.1:4141

# 数据/配置目录（账号、密钥、设置、会话等）。默认 <run.bat 所在目录>\data
# M365_DATA_DIR=D:\path\to\m365-copilot2api\data

# 上游出网代理（仅 M365 上游需要走代理时填写）
# HTTPS_PROXY=http://127.0.0.1:7890
# HTTP_PROXY=http://127.0.0.1:7890
