# 1.6.33-0003

修复普通聊天与 `/v1/images/generations` 生图在微软返回结构化额度错误时反复使用同一账号、最终等待 5 分钟超时的问题。

- 识别不同版本的 `ImageGen*` 每日额度错误，并将账号加入生图冷却。
- 生图轮换会跳过已耗尽生图额度的账号。
- 系统容量不足仍按临时故障处理，不会误判为每日额度耗尽。
- 增加额度错误识别回归测试。

升级后已耗尽额度的账号会自动跳过，其他账号仍可继续尝试生图。
